Build instructions for ParallelCloud:

1. Import parallelcloud and ParallelCloudWeb into Eclipse workspace and do maven update on both projects.
2. Change corresponding file paths in WordsReaderHelper.java, YelpBean.java and Yelp.java
3. Assuming maven is installed, go to command line and cd into workspace/parallelcloud. 
4. Do mvn clean compile assembly:single. A JAR will be generated in parallelcloud/targer.
5. Copy this JAR into ParallelCloudWeb/target. 
6. Add ParallelCloudWeb to Tomcat and start server. Go to index.xhtml and click run on server. 
7. Start MapReduce job once from the webpage
8. Train the values once.
9. Enter review to get a prediction.
